<div class="keyboard num">
    <% for (let c of '123456789') { %>
        <button class="num"><%= c %></button>
    <% } %>

    <button class="delete">Del</button>
    <button class="num">0</button>
    <button class="return">OK</button>
</div>

<div class="keyboard char">
    <div class="row">
        <% for (let c of '1234567890') { %>
            <button class="num"><%= c %></button>
        <% } %>

        <button class="delete func">DEL</button>
    </div>

    <div class="row">
        <% for (let c of 'qwertyuiop') { %>
            <button class="char"><%= c %></button>
        <% } %>
    </div>

    <div class="row">
        <button class="caps func">CAPS</button>

        <% for (let c of 'asdfghjkl') { %>
            <button class="char"><%= c %></button>
        <% } %>

        <button class="return func">RTRN</button>
    </div>

    <div class="row">
        <button class="shift func">SHFT</button>

        <% for (let c of 'zxcvbnm') { %>
            <button class="char"><%= c %></button>
        <% } %>

        <button class="shift func">SHFT</button>
    </div>

    <div class="row">
        <button class="space func">SPACE</button>
    </div>
</div>
